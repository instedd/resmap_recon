%ul.unstyled
  %fieldset
    %div
      %label Name
      %span.value {{site.name}}

    %div
      %label source list
      %span.value {{site.source_list.name}}

    %div{'ng-repeat'=>'(key, value) in site.properties', 'ng-hide'=>"key[0] == '_'"}
      %label {{key}}
      %span.value {{value}}
