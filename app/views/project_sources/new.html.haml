= link_to '← back to project', project_path(@project)

%h1
  New facility source

= simple_form_for @new_source_list, url: project_sources_path(@project) do |f|

  = f.input :name
  = f.input :file

  = submit_ladda_button 'Upload & continue'
